export interface ResponseAPI {
    success: boolean;
    message: string;
    data: any
}

export interface InitialStateINT {
    loading: boolean,
    success: boolean;
    message: string;
    data: any
}